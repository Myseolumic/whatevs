package server;

public class Map {

    public static char[][] generateMap(int size) {
        char[][] map = new char[size][size];
        // get fillet bby
        // tile interfacei kasutada, mitte char[][], nt Tile[][] v mdea misiganes javale meeldib

        return map;
    }

    public static char[][] reduceMapSize(char[][] map) {
        // magic happens
        return map;
    }
}
